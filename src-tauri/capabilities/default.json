{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "NovaEnv 主窗口默认能力：核心 API + updater/process 插件 + 应用自定义 commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default"
  ]
}
